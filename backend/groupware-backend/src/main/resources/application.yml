spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flokr_groupware
    username: root
    password: root123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

logging:
  level:
    com.flokr.groupware: DEBUG
# JWT 설정
jwt:
  secret: flokr-groupware-jwt-secret-key-for-authentication-system-very-long-secret-key-must-be-at-least-256-bits
  token-validity-in-seconds: 86400  # 24시간
